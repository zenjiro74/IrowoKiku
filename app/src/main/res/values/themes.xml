<resources>
    <!-- Compose 側で MaterialTheme を組み立てるため、View 側のテーマは最小限に留める。 -->
    <style name="Theme.IrowoKiku" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
